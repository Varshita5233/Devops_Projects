name: Build and Deploy to EKS

on:
  push:
    branches: [ main ]
    paths:
      - 'eks-demo/**'
      - '.github/workflows/deploy.yml'

env:
  AWS_REGION: ap-south-1
  ECR_FRONTEND: myapp
  ECR_BACKEND: backend
  EKS_CLUSTER: demo-cluster

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Get the code
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Authenticate to AWS using the secrets you set in GitHub
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 3. Log in to ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 4. Build the image, tag it with the commit SHA (immutable!), push to ECR
      - name: Build and push frontend
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_FRONTEND:$IMAGE_TAG ./eks-demo/app
          docker push $ECR_REGISTRY/$ECR_FRONTEND:$IMAGE_TAG
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "ECR_REGISTRY=$ECR_REGISTRY" >> $GITHUB_ENV

      - name: Build and push backend
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_BACKEND:$IMAGE_TAG ./eks-demo/backend
          docker push $ECR_REGISTRY/$ECR_BACKEND:$IMAGE_TAG
          echo "ECR_REGISTRY=$ECR_REGISTRY" >> $GITHUB_ENV
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

      # 5. Point kubectl at the EKS cluster
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig --region $AWS_REGION --name $EKS_CLUSTER

      # 6. Deploy the BACKEND first (frontend depends on it)
      - name: Deploy backend with Helm
        run: |
          helm upgrade --install backend ./eks-demo/helm/backend \
            --namespace prod \
            --create-namespace \
            --set image.repository=$ECR_REGISTRY/$ECR_BACKEND \
            --set image.tag=$IMAGE_TAG \
            --wait --timeout 5m --atomic

      # Deploy with Frontend
      - name: Deploy with Helm
        run: |
          helm upgrade --install myapp ./eks-demo/helm/myapp \
            --namespace prod \
            --create-namespace \
            -f ./eks-demo/helm/myapp/values-prod.yaml \
            --set image.repository=$ECR_REGISTRY/$ECR_FRONTEND \
            --set image.tag=$IMAGE_TAG \
            --wait --timeout 5m --atomic

      # 7. Show what got deployed
      - name: Verify deployment
        run: |
          kubectl get pods -n dev
          kubectl get svc -n dev
          kubectl get ingress -n dev
